{
  "name": "minhas-financas",
  "version": "1.0.0",
  "description": "Controle de finanças pessoais — servidor Node + SQLite nativo, sem dependências.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node --experimental-sqlite server.js"
  }
}
